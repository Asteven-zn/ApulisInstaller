group_labels:
  - group: kube-master
    index: 0
    labels:
      - mysql=active
      - postgres=active

  - group: kube-master
    labels:
      - grafana=active
      - prometheus=active
      - aiarts-backend=active
      - aiarts-frontend=active
      - alert-manager=active
      - image-label=active
      - jobmanager=active
      - restfulapi=active
      - webui=active
      - nginx=active
      - watchdog=active
      - webportal=active
      - mlflow=active

  - group: kube-worker
    labels:
      - worker=active
      - FragmentGPUJob=active
