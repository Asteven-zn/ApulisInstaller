apiVersion: v1
kind: PersistentVolume
metadata:
  name: aiplatform-app-data-pv
spec:
  capacity:
    storage: 300Mi
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  mountOptions:
  {% for option in default_mount_opts %}
    - {{ option }}
  {% endfor %}
  nfs:
    server: {{groups['nfs-server'][0]}}
    path: {{ nfs_pv_pvc_mounts["app-data"]["nfs_path"] }}

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: aiplatform-app-data-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
