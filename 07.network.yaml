# to install network plugin, only one can be choosen
- hosts:
    - kube-master
    - kube-worker
  roles:
    - { role: weave, when: "CLUSTER_NETWORK == 'weave'" }

- hosts:
    - kube-master
  roles:
    - kube-vip