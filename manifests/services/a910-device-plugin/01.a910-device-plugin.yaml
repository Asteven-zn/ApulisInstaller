{% import 'macros/common.j2' as util with context %}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: a910-device-plugin
  namespace: kube-system
  labels:
    app: a910-device-plugin
spec:
  selector:
    matchLabels:
      app: a910-device-plugin
  template:
    metadata:
      labels:
        app: a910-device-plugin
    spec:
      {{ util.dns_policy() }}
      hostNetwork: true
      nodeSelector:
        gpuType: huawei_npu_{{ arch_map[ansible_architecture] }}
      containers:
        - name: a910-device-plugin
          image: {{ util.image_name('a910-device-plugin', 'third') }}
          imagePullPolicy: Always
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          securityContext:
            # readOnlyRootFilesystem: true
            privileged: true
            # allowPrivilegeEscalation: false
          volumeMounts:
            - name: devfs
              mountPath: /dev
              # readOnly: true
            - name: sysfs
              mountPath: /sys/class/devdrv-class
              readOnly: true
            - name: kubeletsockets
              mountPath: /var/lib/kubelet/device-plugins
            - name: hccn
              mountPath: /etc/hccn.conf
      volumes:
        - name: devfs
          hostPath:
            path: /dev
        - name: sysfs
          hostPath:
            path: /sys/class/devdrv-class
        - name: kubeletsockets
          hostPath:
            path: /var/lib/kubelet/device-plugins
        - name: hccn
          hostPath:
            path: /etc/hccn.conf
